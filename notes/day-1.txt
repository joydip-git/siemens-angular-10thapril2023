https://github.com/joydip-git/siemens-angular-10thapril2023

Angular:

1. JavaScript - basic and advanced
2. TypeScript
3. New features in TS/JS 
4. web app development using JS frameworks/libraries


var x = 10


var x:number=10
x='joydip'

angular-apps

HTML:
self-closing/empty tag: which does not have any nested tag/element or text inside it.
<tag-name /> or <tag-name>


web servers:
IIS (internet information services)
Apache Tomcat
NGINX
VERCEL


Development mode:
App Development server: host one application at a time, using port number
lite-server
Live Server
Webpack Development Server etc.

CSS:
Cascading Style Sheet

a collection of properties and values

a. inline styling
b. outline styling
	i.  in the same HTML file
	ii. in separate CSS file

selectors:
a. element 
b. id
c. class

text-align: center
background-color:grey


typeScript:
data types: number, string, boolean, object [any, partial]

>npm init (npm init --yes) => will create package.json file
>.\node_modules\.bin\tsc --init => will create configuration file for typescript transpiler configuration (tsconfig.json)

module formats:

import/export (ES6 module format)
require() (common js format - default for node js applications)
amd (asynchronous module definition format) => requirejs understands this format
umd (universal module definition format)


module loaders:
requirejs

packages:
a. installed in an application
b. installed globally in the system

Tool to create angular application: 
Angular CLI
package name: @angular/cli
>npm i @angular/cli -g
global path: C:\Users\<user-name>\AppData\Roaming\npm

Angular assets and decorators:

1. component @Component()
2. directive @Directive()
3. pipe @Pipe()

4. service @Injectable()

5. module @NgModule()







